const Header = () => {
    const template = `
        <header>
            <h1>To Do List</h1>
            <p>Add, create or delete your to do items for the day!</p>
        </header>
    `;

    return template;
}

export default Header;